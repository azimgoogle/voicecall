name: voice_call_poc
description: "WebRTC audio calling POC"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.12.1
  firebase_database: ^11.3.4
  flutter_webrtc: ^0.12.9
  shared_preferences: ^2.5.3
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
